spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:9000
      name: common, app-reply, topic

---

spring:
  profiles: prod
  cloud:
    config:
      uri: http://config.okky.io:9000